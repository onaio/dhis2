{ "indicatorGroup": 
  {
    "id": $!{indicatorGroup.id},
    "name": "$!encoder.jsonEncode( ${indicatorGroup.displayName} )",
    "alternativeName": "$!encoder.jsonEncode( ${indicatorGroup.alternativeName} )",
    "memberCount": "$memberCount"
  }
}
